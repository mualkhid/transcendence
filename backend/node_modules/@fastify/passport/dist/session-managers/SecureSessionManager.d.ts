import { FastifyRequest } from 'fastify';
import { AuthenticateOptions } from '../AuthenticationRoute';
import { SerializeFunction } from '../Authenticator';
import { FastifySessionObject } from '@fastify/session';
import { Session, SessionData } from '@fastify/secure-session';
type Request = FastifyRequest & {
    session: FastifySessionObject | Session<SessionData>;
};
export declare class SecureSessionManager {
    key: string;
    clearSessionOnLogin: boolean;
    clearSessionIgnoreFields: string[];
    serializeUser: SerializeFunction;
    constructor(serializeUser: SerializeFunction);
    constructor(options: {
        key?: string;
        clearSessionOnLogin?: boolean;
        clearSessionIgnoreFields?: string[];
    }, serializeUser: SerializeFunction);
    logIn(request: Request, user: any, options?: AuthenticateOptions): Promise<void>;
    logOut(request: Request): Promise<void>;
    getUserFromSession(request: Request): any;
}
export {};

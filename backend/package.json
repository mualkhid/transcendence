{
  "name": "transcendence",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "echo \"⚠️  Use 'make up-logs' for development with Docker\" && exit 1",
    "start": "echo \"⚠️  Use 'make up' to start with Docker\" && exit 1",
    "studio": "echo \"⚠️  Use 'make backend-shell' then 'npx prisma studio'\" && exit 1",
    "migrate": "echo \"⚠️  Use 'make migrate' for database migrations\" && exit 1",
    "generate": "echo \"⚠️  Use 'make generate' to generate Prisma client\" && exit 1",
    "docker:dev": "nodemon server",
    "docker:start": "node server",
    "docker:studio": "prisma studio",
    "docker:migrate": "prisma migrate dev",
    "docker:generate": "prisma generate"
  },
  "keywords": [],
  "author": "",
  "type": "module",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.1",
    "@fastify/multipart": "^9.0.3",
    "@fastify/passport": "^3.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@fastify/websocket": "^11.2.0",
    "@prisma/client": "^6.14.0",
    "bcrypt": "^6.0.0",
    "fast-glob": "^3.3.3",
    "fastify": "^5.5.0",
    "fastify-swagger": "^5.1.1",
    "file-type": "^21.0.0",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^3.3.2",
    "qrcode": "^1.5.4",
    "sanitize-html": "^2.17.0",
    "speakeasy": "^2.0.0",
    "validator": "^13.15.15",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.14.0"
  }
}
